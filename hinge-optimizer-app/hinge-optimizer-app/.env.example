# Copie ce fichier en ".env" pour le développement local (jamais commité sur Git).
# En production (Vercel/Netlify), cette variable se configure dans le dashboard
# de l'hébergeur, pas dans un fichier.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
